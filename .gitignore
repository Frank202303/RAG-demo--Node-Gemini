# Node.js dependencies
/node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by coverage tools
coverage/

# Optional npm cache directory
.npm
.eslintcache

# dotenv environment variables file
.env
.env.local
.env.*.local

# GCP 服务账号密钥（切勿提交）
key.json
*-sa.json

# VSCode settings
.vscode/

# Mac system files
.DS_Store

# Windows system files
Thumbs.db
ehthumbs.db
Desktop.ini

# Build outputs
dist/
build/

# 不要忽略 package-lock.json：gcloud deploy --source 会按 .gitignore 打包，
# 若缺少 lock 文件，Dockerfile 里 npm ci 会在 Cloud Build 中失败。
yarn.lock
pnpm-lock.yaml